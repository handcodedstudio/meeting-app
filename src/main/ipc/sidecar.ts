// SIDECAR_HEALTH and PYANNOTE_ENSURE are registered in transcribe.ts so the
// transcription sub-agent owns them as a unit. This file is intentionally empty.
export function registerSidecarHandlers(): void {
  /* no-op */
}
